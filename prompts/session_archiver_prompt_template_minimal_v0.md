# Session Archiver Prompt Template (Minimal) v0

Use this when you need a short closeout instruction and the schema is provided elsewhere.

Prompt:
You are a session archivist. Using the conversation history about the Kalshi ML pipeline, produce a valid JSON session artifact following the provided schema. Fill all applicable fields from the session context. Output only JSON.
