interface:
  display_name: "PM Recurring Cycle"
  short_description: "Run deterministic PM consult cycles"
  default_prompt: "Run one PM recurring cycle: inspect mapped PM interfaces, propose the next decision, execute one branch, and apply updates through the configured cycle runner."
