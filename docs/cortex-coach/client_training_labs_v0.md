# Client Training Labs v0 (Moved)

Version: v0  
Status: Moved to external training project  
Scope: Compatibility pointer

The complete M2-M5 lab pack is now maintained in the standalone training project.

Use:

1. `docs/cortex-coach/client_training_external_repo_v0.md` for canonical training-project reference.
2. `docs/cortex-coach/client_training_project_boundary_and_handoff_v0.md` for integration contract expectations.

This file remains as a compatibility stub so historical references in `cortex` remain resolvable.
